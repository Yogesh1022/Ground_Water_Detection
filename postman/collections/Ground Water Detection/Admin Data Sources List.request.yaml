$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/admin/data-sources'
order: 5300
headers:
  - key: Authorization
    value: Bearer {{adminToken}}
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Admin data sources list is 200', function () {
        pm.response.to.have.status(200);
      });
