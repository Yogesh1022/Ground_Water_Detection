$kind: http-request
method: GET
url: '{{baseUrl}}/api/v1/admin/overview'
order: 4000
headers:
  - key: Authorization
    value: Bearer {{adminToken}}
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Admin overview is 200', function () {
        pm.response.to.have.status(200);
      });
