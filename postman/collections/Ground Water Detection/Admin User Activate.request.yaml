$kind: http-request
method: PUT
url: '{{baseUrl}}/api/v1/admin/users/{{testUserId}}/activate'
order: 4600
headers:
  - key: Authorization
    value: Bearer {{adminToken}}
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test('Admin user activate is 200', function () {
        pm.response.to.have.status(200);
      });
